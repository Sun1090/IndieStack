/**
 * 仪表盘首页加载骨架（G04：统一走共享 PageLoading）
 */

import { PageLoading } from "@/components/shared/page-loading";

export default function DashboardLoading() {
  return <PageLoading variant="dashboard" />;
}
